//Handles the Day form

import { addDay, getDays, clearDays } from './DayHandler.mjs';

const dayForm = document.getElementById('day-creation')
const dayDisplay = document.getElementById('days-display')

dayForm.addEventListener('submit', (insertDay) => {
    insertDay.preventDefault();

    const dayDate = document.getElementById('date')
    const hours = document.getElementById('hours').value.trim();

    try{
        const 
    }

});